apiVersion: v1
kind: Service
metadata:
  name: {{ include "Botpress.fullname" . }}
  labels:
    {{- include "Botpress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Botpress.selectorLabels" . | nindent 4 }}
